name: Ruby

on:
  push:
    branches:
      - main

  pull_request:

# comment
jobs:
  build:
    runs-on: ubuntu-latest # comment

    strategy:
      matrix:
        ruby:
          - 3.0.2   # comment